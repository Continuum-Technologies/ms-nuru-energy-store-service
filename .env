# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL=postgresql://postgres:password@127.0.0.1:5434/nuru_db?schema=nuru

# RustFS (local S3-compatible object storage) — run via `docker compose up -d rustfs`
RUSTFS_ENDPOINT=http://localhost:9000
RUSTFS_ACCESS_KEY=rustfsadmin
RUSTFS_SECRET_KEY=rustfsadmin
RUSTFS_BUCKET=nuru-media
RUSTFS_PUBLIC_URL=http://localhost:9000
# Not read at runtime after the initial seed.
OWNER_EMAIL="lisper@gmail.com"
OWNER_PASSWORD="Shamirjamal@2"